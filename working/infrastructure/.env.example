# Backend Lambda Environment Variables
# Set these in: Lambda → Configuration → Environment variables
# For local dev with SAM: copy to backend/.env.local

# ─── AWS Region ───────────────────────────────────────────────────────────────
AWS_REGION=ap-south-1

# ─── DynamoDB Tables ──────────────────────────────────────────────────────────
DYNAMODB_IMAGES_TABLE=ai-bharat-images
DYNAMODB_CHATS_TABLE=ai-bharat-chats
DYNAMODB_USERS_TABLE=ai-bharat-users

# ─── S3 Storage ───────────────────────────────────────────────────────────────
S3_BUCKET_NAME=ai-bharat-fish-images

# ─── Amazon Cognito ───────────────────────────────────────────────────────────
COGNITO_USER_POOL_ID=ap-south-1_XXXXXXXXX
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── ML API (plug in your real endpoint) ──────────────────────────────────────
# Expected POST body:  { s3Path: string }
# Expected response:   { species, scientificName, qualityGrade, confidence,
#                        measurements, compliance, marketEstimate }
ML_API_URL=https://your-ml-api-endpoint.example.com/analyze
ML_API_KEY=your-ml-api-key-here

# ─── Agentic Chat API ─────────────────────────────────────────────────────────
# Expected POST body:  { userId, message, sessionId }
# Expected response:   { response: string }
CHAT_API_URL=https://your-chat-api-endpoint.example.com/chat
CHAT_API_KEY=your-chat-api-key-here

# ─── API Gateway ──────────────────────────────────────────────────────────────
# Set this on the FRONTEND side after deploying your API
# NEXT_PUBLIC_API_URL=https://xxxxxxxxxx.execute-api.ap-south-1.amazonaws.com/prod
